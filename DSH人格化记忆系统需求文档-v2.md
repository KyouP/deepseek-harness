# DSH 人格化记忆系统（H-MEM）需求文档 v2

- 版本：v2.0
- 日期：2026-08-19
- 状态：草案（基于 v1.1 实现复盘 + 四个参照系统对比分析）
- 项目代号：H-MEM（Hybrid Persona Memory），基于 DSH 插件化架构
- 前版：[DSH人格化记忆系统需求文档.md](DSH人格化记忆系统需求文档.md)（v1.0，2026-08-18）

---

## 0. 修订说明

v2 相对 v1 的变化来自两个信息源：

1. **v1.1 实现复盘**：v1 已交付并实测（见 §4 进度评判），哪些需求定级过于乐观、哪些设计被验证有效，有了实证依据。
2. **四个参照系统的对比分析**（见 §3）：
   - **dsh-memory-evolve**（csyangwen）：五轨 Markdown 记忆 + 确认制建议队列 + 到期粘性审查 + 前缀缓存工程化
   - **dsh-auto-memory**（Aik358）：每轮自动沉淀（turn-stopping + subagent 抽取）+ 写入卫生闸门 + 静态/动态注入分离
   - **Hermes Agent**（Nous Research）：有界常驻记忆 + FTS5 会话检索 + 容量上限即遗忘阀门
   - **Mem0**（mem0ai）：LLM 抽取管线 + ADD/UPDATE/DELETE 裁决 → v2 演进为 ADD-only + 检索端衰减

**核心结论：v1 文档的整体架构方向（分层、双时间、两级表示、承诺特权通道、降级链）被参照系统交叉验证为合理；主要偏差在（a）写入端过度依赖模型自觉、（b）低估了 DSH 注入机制对前缀缓存的影响、（c）缺少写入卫生/安全需求、（d）FR-5.x 多数超出插件层能力边界、（e）部分 P0/P1 定级与实际实现难度倒挂。**

---

## 1. 项目背景与目标（继承 v1，未变）

### 1.1 背景

DSH 是「一切皆插件」的 agent 运行时。本项目以记忆子系统为切入点，构建具备真实人格形象的拟人化 agent；人格稳定感主要来自记忆连续性。

### 1.2 总体目标

「超人级可靠性 + 类人级体感 + 工程级性能约束」的混合记忆系统：记忆精确、可备份、可精确删除（优于人）；有选择记忆、会淡忘、会联想（接近人）；热路径有硬预算、重活全在暗处（工程约束）。

### 1.3 三条设计公理（未变）

1. **上下文是有损显示层，存储是无损真相层** —— 离开上下文的内容先落存储，指针随行。
2. **一切注入物都有价格，预算装箱，高优先级不可挤压** —— 不存在无界注入路径。
3. **丢失的判定标准是「找不回」，不是「想不起」** —— 想不起是召回率问题；找不回是可靠性事故。

### 1.4 v2 新增第四条公理（来自参照系统教训）

4. **写入端必须有卫生闸门与信任边界** —— 记忆内容会回流 prompt（注入面=攻击面），乱码/复读/注入指令/敏感段在入库与注入两个关口都要过滤。auto-memory 的"语义朊病毒"事故链与 Mem0 文献记录的对抗污染风险证明这不是理论问题。

---

## 2. 名词定义（继承 v1，新增三条）

| 术语 | 定义 |
|---|---|
| M0 工作记忆 | 当前会话的对话原文 + 会话便签（scratchpad），生命周期为会话 |
| M1 核心记忆 | 常驻上下文的 persona block 与 human block，模型可自编辑 |
| M2 情节记忆 | 事件卡片网络，卡片间语义互链，带显著性与强度 |
| M3 语义记忆 | 双时间事实图谱（变化而非覆盖） |
| M4 深时记忆 | 完整历史与归档记忆，仅以指针形式出现于上下文 |
| 热/温/冷路径 | 响应前同步 / 响应后异步 / 空闲离线批处理 |
| 睡眠巩固 | 空闲时蒸馏、冲突消解、链接演化、衰减结算的后台流程 |
| 承诺追踪器 | 独立特权通道，记录并闭环「自己答应过的事」 |
| 锚点（Pin） | 以原文钉在上下文中、不参与压缩的关键信息 |
| 两级表示 | 每条记忆同时保存「一行摘要」与「全文」，默认只注入摘要 |
| **自动沉淀**（新） | 每轮对话结束后由温路径自动判断并提炼入库，不依赖模型自觉调工具 |
| **确认队列**（新） | AI 提议、人确认的写入缓冲带，带去重与频次信号 |
| **写入卫生闸门**（新） | 入库前的内容过滤：乱码、复读退化、注入指令、敏感段 |

---

## 3. 参照系统对比分析

### 3.1 四系统一句话画像

| 系统 | 存储 | 写入者 | 检索 | 遗忘/整理 | 常驻注入 |
|---|---|---|---|---|---|
| dsh-memory-evolve | Markdown 文件（§ 分隔，五轨） | 模型每轮纪律 + 确认队列 | 全量注入 + 关键词过滤 | 归档（人工/AI 触发），无衰减 | user/memory/key 三轨全量 |
| dsh-auto-memory | Markdown 文件（分层 + 日历） | 每轮自动沉淀（subagent 抽取） | 关键词行扫描 + subagent 扩词 | 30 天蒸馏 + 预算超自动压缩 | 2400 字符预算四等分 |
| Hermes Agent | Markdown（硬上限）+ SQLite/FTS5 | agent 自管理工具 | FTS5 全文检索 | 容量上限强制自我巩固 | MEMORY.md+USER.md 全量 ~1.3-2.2k tok |
| Mem0 | 向量库 + 可选 Neo4j 图 | 后台 LLM 抽取管线 | embedding top-k（+图遍历） | v1 ADD/UPDATE/DELETE 裁决 → v2 ADD-only + 检索端衰减 | 按需 top-k，无常驻 |

### 3.2 对 v1 设计的验证（参照系统支持 v1 的部分）

| v1 设计 | 参照证据 |
|---|---|
| M1 常驻双 block（~1.5k tok） | Hermes 常驻 MEMORY.md+USER.md（~1.3-2.2k tok）+ 冻结保缓存；evolve 全局轨全量注入 |
| SQLite + FTS5 起步 | Hermes 明确选 FTS5 而非向量库：确定性、可审计、零依赖；DSH 内置 |
| 两级表示 + memory_expand | evolve 的渐进式披露（`[summary:]` + expand）已上线验证 |
| 双时间"失效而非删除" | Mem0 v2 演进方向（ADD-only、检索端按时间/衰减排序）与之同构 |
| 降级链（NFR-2.x） | auto-memory 全功能 AI 不可用降级实践印证必要性与可行性 |
| 承诺追踪器 | 差异化设计：四系统均无对等物（evolve 的 dtodo 是通用待办，不闭环"亲口承诺"） |

### 3.3 v1 的缺口（参照系统有而 v1 没有）

| 缺口 | 参照来源 | v2 处置 |
|---|---|---|
| **每轮自动沉淀**：写入完全靠模型自觉调工具，弱遵循模型会漏记 | auto-memory（turn-stopping + 门控链 + subagent 提炼，核心卖点）；evolve（每轮收尾纪律，作者自承弱模型会跳过） | v2 新增 FR-3.5，提前到 v2 期 |
| **前缀缓存工程**：DSH 快照 append-only，动态注入变化 = 截断前缀缓存 = 真金白银；静态纪律与动态数据必须分离 | evolve（注入轨/按需轨划分）+ auto-memory（静态 section 字节稳定做缓存锚） | v2 新增 NFR-3.x |
| **写入卫生闸门**：乱码/复读/注入指令/敏感段过滤 | auto-memory（sanitizeForWrite 五类特征 + 尾部复读去重）；evolve（scanThreat）；Mem0 文献的对抗污染风险 | v2 新增 FR-3.6/3.7 |
| **确认队列**：AI 提议、人确认，去重 + hits 频次信号 | evolve（SUGGESTIONS.jsonl 全链路） | v2 新增 FR-3.8（P2，可选） |
| **轻量审查机制**：计数到期 + 快照醒目警告，主 LLM 回合内静默查重 | evolve（review.js，比睡眠巩固轻一个量级） | v2 将巩固拆为轻量审查（v2）+ 完整巩固（v3） |
| **会话原文检索基础设施**：DSH sessions jsonl 可直接读 / sessionQuery 服务 | auto-memory（discoverWorkspaces + extractSessionText）；Hermes L3 | v2 明确 M4 的两条实现路径 |
| **程序性记忆（技能沉淀）** | Hermes L2 skills/*.md；evolve skill_manage | 不重复造：DSH 已有 SKILL.md 体系，v2 仅列为外围集成点（P2） |
| **多工作区串扰** | auto-memory 单引擎 state 踩坑；evolve cwd 隔离 + 分支过滤 | v2 新增 FR-2.9：人格全局共享、项目记忆按 cwd 隔离（P2） |

### 3.4 v1 的过度设计（参照系统证明可以简化）

| v1 设计 | 参照证据 | v2 处置 |
|---|---|---|
| FR-4.1 三路混合召回（向量+FTS+图）为 P1 | Hermes 纯 FTS5 够用；evolve/auto-memory 连 FTS 都没有也在用；向量是 Mem0 云端库的方案，本地小模型 embedding 质量/延迟未验证 | 向量召回降为 P2 增强；v2 主线 = FTS+LIKE+图邻域+强度加权 |
| FR-3.1 四因子显著性公式 | 无参照系统实现公式化评分；auto-memory 用"最小字符数+每日上限+冷却"门控链达到同样目的且更可控 | 公式保留为目标，v2 先用规则门控链（工程量 1/5） |
| FR-8.2 六段睡眠巩固一次到位 | evolve 的轻量审查已覆盖"查重+提炼"核心价值 | 拆分：轻量审查 v2，完整巩固 v3 |
| 写端冲突消解（v3 计划） | Mem0 从 UPDATE/DELETE 裁决演进到 ADD-only：写端裁决性价比低 | v2 明确 ADD-only + supersede 标记 + 检索端时间排序，不做写端 LLM 裁决 |

---

## 4. v1.1 实现进度评判

评判对象：`packages/memory/{memory-store, memory-core, bundle}`（主仓 commit c740c91377，独立仓 @KyouP/dsh-improve-memory@1.0.0）。

### 4.1 已实现（✓ = 完全达到需求描述）

| 需求 | 状态 | 说明 |
|---|---|---|
| FR-1.2 persona 常驻 | ✓ | system prompt section 注入，无 token 预算强制（见 4.3） |
| FR-1.3 自编辑 persona | ✓ | `memory_update_core`，revision 递增 |
| FR-2.1 scratchpad | ✓（部分） | 表 + `memory_note` 工具 + 24h 窗口注入；「会话结束交巩固」因巩固未实现而悬空 |
| FR-2.2 human block | ✓ | 同 persona，含 config 种子 |
| FR-2.6 原始记录不可变 | ✓ | cards insert-only + 派生列白名单（strength/archived/contextDesc） |
| FR-4.4 两级表示 | ✓ | summary（首行 60 字）+ `memory_expand` 取全文 |
| FR-6.1 承诺表 | ✓ | 独立表 + 同内容去重（v1.1 修复） |
| FR-7.3 精确遗忘 | ✓ | `memory_forget` 事务级联删 facts→links→card |
| FR-10.1/10.2/10.4/10.5 | ✓ | recall/expand/store/update_core 四工具在线，另有计划外 `memory_note` |
| NFR-2.1/2.2/2.3 降级链 | ✓ | 注入异常渲染空串；无 embedding 依赖；库打不开整体降级"无记忆"不拖垮宿主 |
| 部署约束 | ✓ | 纯 ESM bundle、SQLite WAL、零外部服务、`dsh plugin add` 可装 |

### 4.2 部分实现（结构在、机制空转）

| 需求 | 缺口 |
|---|---|
| FR-1.1 结构化角色卡 | persona 是自由文本 block，无性格/价值观/口癖/边界结构化字段 |
| FR-2.3 M2 卡片结构 | 列全（内容/上下文/关键词/情绪/显著性/强度/双时间戳），但 links 表无任何写入 API（死表）、salience 恒 1、emotion 无写入路径 |
| FR-2.5 双时间事实 | facts 表 + `insertFact`/`supersedeFact`/`activeFacts` 存储层就绪，但无工具暴露、无流程调用（仅测试覆盖） |
| FR-3.3 显式「记住」必入库 | 工具可达且 pinned 默认 true，但触发完全靠模型自觉（v1.1 已用工具描述纠偏缓解） |
| FR-6.2 承诺注入 P0 不可挤压 | 注入 order 最高 + cap 20 行，但全局预算体系不存在，「不可挤压」无对立面可验证 |
| FR-6.3 到期浮出 | 注入标注「【到期，请主动提起】」，但无唤醒预热流程，模型是否提起仍靠自觉 |
| FR-7.4 pin 不衰减 | pinned 列存在，但衰减机制不存在，「不衰减」目前是空头背书 |
| 召回质量 | FTS5 bm25 + CJK LIKE 兜底（v1.1），但排序不消费 salience/strength/pinned/recency，无相关性下限截断 |

### 4.3 未实现（✗）

| 需求 | 说明 |
|---|---|
| FR-1.4 角色卡 bundle 分发 | P1，未做 |
| FR-2.4 自动语义链接 / 链接演化 | 死表，P2 |
| FR-2.7 M4 会话原文取回 / FR-10.3 `memory_browse` | 工具不存在，全仓无 browse |
| FR-2.8 导出/备份/迁移 | P2 |
| FR-3.1/3.2 显著性评分与三档门控 | 写入恒 salience=1 |
| FR-3.4 低置信「不确定」标注 | recall 返回的 uncertain 字段恒 false（占位） |
| FR-4.1/4.2/4.3/4.5/4.6/4.7 混合召回/排序公式/漏斗/预算装箱/相关性下限/聚焦压缩 | 全部未实现；预算控制仅有承诺 20 行、便签 24h/20 条两个硬 cap |
| FR-5.1-5.6 工作记忆上下文管理（三段式/增量压缩/压缩前抽取/外部化/任务栈/锚点） | **全部未实现，且多数依赖 DSH 核心会话管理能力，插件层无法独立交付**（见 §5.5 重新分类） |
| FR-7.1/7.2 衰减/归档/复苏 | 列与白名单就绪，无执行器 |
| FR-8.x 睡眠巩固全部 | 无定时器、无会话生命周期 hook |
| FR-9.1 唤醒预热 | 无会话开始 hook |
| 承诺闭环 | `closeCommitment` 只在测试里被调，模型无工具关闭承诺（**闭环断裂，FR-6 系列的关键缺口**） |
| persona/human 注入预算 | 双 block 无任何截断，用户可写爆（公理二的缺口） |

### 4.4 进度总评

**v1 文档宣称的 v1 范围（M1 双 block + 自编辑 + scratchpad + 承诺追踪 + 两级表示 + 三段式上下文骨架）中，前三项完整交付，承诺追踪交付但闭环断裂，两级表示交付，三段式上下文骨架未交付（且被重新定性为 DSH 核心协同需求）。** v1 文档对插件层能力边界的估计偏乐观：凡是要介入 DSH 会话上下文组装流程的需求（FR-5.x 全部、FR-4.5 的全局预算、FR-9.1 的唤醒 hook），都需要先确认 DSH 是否暴露对应扩展点。

---

## 5. 修正后的功能需求（v2 全量）

优先级重定义：**P0 = 下一迭代（v2 期）必须；P1 = v3 期；P2 = v4+ 或增强项**。每条标注 v1.1 实现状态（✓/半/✗）。

### 5.1 人格定义层

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| FR-1.1 | 结构化角色卡（性格、价值观、口癖、行为边界、禁忌话题）作为人格事实来源 | P1 | 半（自由文本 block） |
| FR-1.2 | persona block 常驻上下文，**带字符预算硬截断**（默认 3000 字符，可配） | P0 | 半（常驻✓，截断✗） |
| FR-1.3 | 模型可工具自编辑 persona block | P0 | ✓ |
| FR-1.4 | 角色卡可打包为独立 bundle 分发 | P2 | ✗ |
| FR-1.5（新） | human block 同样带字符预算硬截断（默认 2500 字符） | P0 | ✗ |

### 5.2 记忆分层存储

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| FR-2.1 | M0 scratchpad（24h 窗口注入） | P0 | ✓ |
| FR-2.2 | M1 human block（画像 + 关系状态） | P0 | ✓ |
| FR-2.3 | M2 卡片结构（内容/上下文/关键词/情绪/显著性/强度/链接/双时间戳） | P0 | 半（结构✓，链接/情绪/salience 空转） |
| FR-2.4 | 新卡片入库自动与历史卡片建立链接（v2 用关键词/共现规则建链，不用向量） | P1 | ✗ |
| FR-2.5 | M3 双时间事实：**ADD-only + supersede 标记**，检索端按时间排序，不做写端 LLM 冲突裁决（Mem0 v2 路线） | P1 | 半（存储层✓） |
| FR-2.6 | 原始记录不可变，派生解释可更新 | P0 | ✓ |
| FR-2.7 | M4：会话原文可经 `memory_browse` 按会话/时间范围取回。**实现路径二选一**：(a) 读 DSH sessions jsonl（auto-memory 已示范）；(b) 消费 sessionQuery 服务（若部署）。优先 (a)，零依赖 | P1 | ✗ |
| FR-2.8 | 导出/备份/迁移（SQLite 单文件天然可拷贝，提供导出工具即可） | P2 | ✗ |
| FR-2.9（新） | 作用域规则：persona/承诺/全局记忆全工作区共享；项目相关记忆按 cwd 打标隔离，注入时过滤 | P2 | ✗（当前全部全局） |

### 5.3 写入门控

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| FR-3.1 | 显著性评分四因子公式 | P1 | ✗（v2 先用 FR-3.5 规则门控链替代） |
| FR-3.2 | 三档处理（丢弃/暂存/入库） | P1 | ✗ |
| FR-3.3 | 用户显式「记住」必入库且 pin | P0 | 半（工具✓，触发靠模型自觉） |
| FR-3.4 | 低置信事实标「不确定」，注入带前缀 | P1 | ✗（字段占位恒 false） |
| **FR-3.5（新）** | **每轮自动沉淀（温路径）**：`agent/turn-stopping` 触发，门控链（防重入 → 最小字符数 → 每日上限 → 冷却 → 按轮去重）→ subagent 提炼 → 按 `[TOPIC]/[CARD]/[FACT]/[USER]` 标记分流入库；失败入重试队列。参照 auto-memory consolidateTurn | P0 | ✗ |
| **FR-3.6（新）** | **写入卫生闸门（入库关）**：拒写乱码特征、复读退化（词/字连读阈值）、raw JSON envelope、base64 残骸、连续重复行；单条长度上限；与近尾条目复读去重。参照 auto-memory sanitizeForWrite | P0 | ✗ |
| **FR-3.7（新）** | **注入卫生闸门（出库关）**：注入前剥离注入指令模式（中英"忽略之前指令"类）、敏感段（标题含凭据/token/密钥/password 的段整段不注入） | P0 | ✗ |
| FR-3.8（新） | 确认队列（可选模式）：自动沉淀的高轨写入（M1/全局 M3）先入建议队列，用户确认后转正；同内容合并累计 hits | P2 | ✗ |

### 5.4 记忆召回

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| FR-4.1 | 召回通道：FTS5 + CJK LIKE 兜底（已有）→ v2 加**图邻域扩展**（命中卡的链接邻居 +1 跳）；向量 embedding（bge-m3 via ollama）降为增强项 | P1（图）/ P2（向量） | 半 |
| FR-4.2 | 排序公式：`score = α·bm25 + γ·strength + δ·链接邻近度 + ε·recency`，pinned 加权 | P1 | ✗（纯 bm25） |
| FR-4.3 | 召回漏斗：粗召回 50 → 精排 10（相关性下限截断 + 同事件聚簇去重） | P1 | ✗ |
| FR-4.4 | 两级表示（摘要 + expand） | P0 | ✓ |
| FR-4.5 | 预算控制：v2 采用**分档字符预算**（参照 auto-memory：总注入预算字符数，按区分配），token 级 score/token 贪心装箱作为目标保留 | P0（字符预算）/ P1（token 装箱） | 半（仅有行数 cap） |
| FR-4.6 | 相关性下限：低于阈值不注入，宁缺毋滥 | P1 | ✗ |
| FR-4.7 | 长卡片查询聚焦压缩（温路径预计算） | P2 | ✗ |
| FR-4.8（新） | **自动召回注入**：每轮按最近用户消息自动触发一次召回，结果按预算注入（替换当前静态 hint 占位行）；失败/超时降级为空 | P1 | ✗（静态 hint 占位） |
| FR-4.9（新） | CJK 检索正式化：LIKE 兜底保留；评估 FTS5 trigram tokenizer（SQLite 3.34+ 内置）替代 unicode61，解决句中词与前缀匹配问题 | P1 | 半（LIKE 兜底✓） |

### 5.5 工作记忆上下文管理（**重新分类：DSH 核心协同需求**）

v1 的 FR-5.1-5.6（三段式上下文、增量压缩、压缩前抽取、中间产物外部化、任务栈、锚点 pin）多数要求介入 DSH 会话上下文的组装与压缩流程，**超出纯插件层能力边界**。v2 处理方式：

| 编号 | 需求 | v2 分类 |
|---|---|---|
| FR-5.1 | 三段式上下文 | **核心协同**：需 DSH agent-loop 暴露上下文组装扩展点，先调研后立项 |
| FR-5.2 | 增量压缩 | **核心协同**：依赖 DSH compaction 机制 |
| FR-5.3 | 压缩前抽取 | **核心协同**：需 DSH compaction 前 hook；插件侧配合提供抽取器（复用 FR-3.5 沉淀管线） |
| FR-5.4 | 中间产物外部化 | **核心协同**：需工具结果拦截扩展点（PreToolUse/PostToolUse 已有，可行性较高） |
| FR-5.5 | 任务栈 | 降为 P2，且可纯 prompt 层实现（scratchpad 结构化段） |
| FR-5.6 | 锚点 pin | **插件可实现**：pinned 卡片/承诺常驻注入即等价物，P1 |

### 5.6 承诺追踪器（特权通道）

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| FR-6.1 | 承诺独立成表（内容/对象/期限/状态）+ 同内容去重 | P0 | ✓ |
| FR-6.2 | 活跃承诺注入最高优先级 | P0 | ✓ |
| FR-6.3 | 到期承诺唤醒浮出，角色主动提起（闭环） | P1 | 半 |
| **FR-6.4（新）** | **承诺闭环工具**：`memory_close_commitment(id)` 或 `memory_store` 扩展 action，模型可标记完成/取消；存储层 `closeCommitment` 已就绪，只差暴露 | **P0** | ✗ |
| **FR-6.5（新）** | 被动承诺识别：温路径沉淀管线（FR-3.5）增加承诺识别分流，模型亲口许下的待办自动入表（不靠模型记得调工具） | P1 | ✗ |

### 5.7 遗忘与归档

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| FR-7.1 | 指数衰减 + 访问强化 | P2 | ✗ |
| FR-7.2 | 低于阈值沉 M4（归档不删）、命中可复苏 | P2 | ✗ |
| FR-7.3 | `memory_forget` 精确删除 + 级联 | P0 | ✓ |
| FR-7.4 | pinned 永不衰减 | P2（随 7.1 生效） | 半 |

### 5.8 睡眠巩固（冷路径）—— 拆为两档

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| **FR-8.0（新）** | **轻量周期审查**（参照 evolve review.js）：按用户回合计数到期（默认 5 轮）→ 快照注入醒目提示 → 主 LLM 回合内静默执行查重 + 提炼建议（≤2 条、宁缺毋滥）→ 确认队列或直接入库 | P1 | ✗ |
| FR-8.1 | 巩固触发：会话结束 N 分钟无交互或定时 | P1 | ✗ |
| FR-8.2 | 完整巩固流水线：① 会话蒸馏 ② 冲突消解（supersede 区间）③ 链接演化 ④ human block 重编译 ⑤ 衰减结算 ⑥ 归档/复苏 | P1（①②④）/ P2（③⑤⑥） | ✗ |
| FR-8.3 | 巩固结果人格化表达（次日主动提起） | P2 | ✗ |

### 5.9 唤醒预热

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| FR-9.1 | 会话开始预取：临期承诺、最近未完结话题、纪念日；经 `agent/session-start`（或 pre-step 兜底，auto-memory 已示范）触发 | P1 | ✗（无生命周期 hook） |

### 5.10 工具接口

| 编号 | 工具 | 优先级 | 状态 |
|---|---|---|---|
| FR-10.1 | `memory_recall(query)` | P0 | ✓ |
| FR-10.2 | `memory_expand(id)` | P0 | ✓ |
| FR-10.3 | `memory_browse(session, range)` | P1 | ✗ |
| FR-10.4 | `memory_store(content, ...)` | P0 | ✓ |
| FR-10.5 | `memory_update_core(block, content)` | P0 | ✓ |
| FR-10.6 | `memory_forget(id)` | P0 | ✓ |
| FR-10.7 | `memory_note(text)`（计划外新增，已验证有用） | P0 | ✓ |
| **FR-10.8（新）** | `memory_close_commitment(id, resolution)` | **P0** | ✗ |
| FR-10.9（新） | `memory_pin(id)` / `memory_unpin(id)`（配合 FR-5.6/7.4） | P1 | ✗ |

### 5.11 注入架构（新增小节，来自参照系统教训）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-11.1 | **静态/动态分离**：写入纪律、工具用法等静态文案走 `systemPrompt.section`（字节级稳定，做前缀缓存锚）；动态记忆数据走 `systemPrompt.context`（变更才追加新快照） | P0 |
| FR-11.2 | 动态注入内容分档预算（FR-4.5 字符预算在此执行）：承诺区 / 便签区 / 召回区 / 唤醒预热区各自配额 | P0 |
| FR-11.3 | 高频变化内容（每轮都变的）不注入，改为工具按需读取——保前缀缓存 | P1 |

### 5.12 人格化外围（非本期范围，继承 v1）

生命节律、具身表现、一致性守护：预留 hook，不实现。程序性记忆（技能沉淀）不重复建设，与 DSH SKILL.md 体系集成即可（P2）。

---

## 6. 非功能需求（校准后）

### 6.1 性能

| 编号 | 指标 | 目标值 | 说明 |
|---|---|---|---|
| NFR-1.1 | 热路径同步检索延迟 p95 | ≤ 300ms | 纯 SQLite 检索实测远低于此；Mem0 含 LLM 抽取 p95 1.44s 作对照 |
| NFR-1.2 | 记忆注入总预算 | ≤ 窗口 10% | 经字符预算近似执行 |
| NFR-1.3 | 写入/抽取/巩固全在温/冷路径 | 必须 | 热路径零写入 |
| NFR-1.4 | 感知响应时间增幅 | ≤ 10% | |
| NFR-1.5（新） | 自动沉淀单次成本 | 每日 LLM 调用 ≤ 可配上限（默认 8 次/日），冷却 ≥ 30 分钟 | 参照 auto-memory 门控链，防成本失控 |

### 6.2 可靠性与降级

| 编号 | 需求 | 状态 |
|---|---|---|
| NFR-2.1 | 召回超时 → 仅 M0+M1 回复 | ✓ 已实现 |
| NFR-2.2 | embedding 不可用 → 退化纯 FTS | ✓（当前本无 embedding 依赖；引入向量后须保持） |
| NFR-2.3 | 全部外部依赖失效 → M0+M1 保底 | ✓ 已实现 |
| NFR-2.4 | 指针回溯成功率 ≥ 99% | 待 M4 上线后验收 |
| NFR-2.5 | 承诺闭环率 100% | **当前不可验收**（闭环工具缺失），FR-6.4 落地后恢复 |
| NFR-2.6 | 事实检索准确率 ≥ 95% | 待召回增强后验收 |
| NFR-2.7（新） | 自动沉淀失败 → 重试队列（上限 5）+ 全部功能可降级为纯手动模式 | 新增 |

### 6.3 注入缓存友好（新增）

| 编号 | 需求 |
|---|---|
| NFR-3.1 | 静态 prompt 段字节级稳定：配置不变则渲染结果逐字节相同 |
| NFR-3.2 | 动态注入内容不变时不追加新快照（复用 DSH context 变更检测） |
| NFR-3.3 | 单条注入内容变化引起的快照追加每轮 ≤ 1 条 |

### 6.4 部署约束（继承，未变）

全本地运行；SQLite + FTS5；向量检索可选（ollama bge-m3）且可平滑摘除；纯 ESM 插件 bundle。

---

## 7. 分期实施路线（重排）

```
v1.1（已交付）  M1 双 block + 自编辑 + scratchpad + 承诺表 + 卡片存储
                + FTS/LIKE 召回 + 两级表示 + 精确遗忘 + 降级链
                → 「稳定人格 + 记住用户」体感已成立

v2（下一迭代）  补闭环 + 自动化 + 闸门：
                FR-6.4 承诺闭环工具 · FR-3.5 每轮自动沉淀 · FR-3.6/3.7
                写入/注入卫生闸门 · FR-1.2/1.5 双 block 预算截断
                FR-4.5 分档字符预算 · FR-11.1/11.2 注入架构
                → 记忆从「模型记得用才有」变为「不写不行」，
                  公理二（预算）与公理四（卫生）落地

v3              质量跃升：
                FR-4.8 自动召回注入 · FR-4.2/4.3/4.6 排序与漏斗
                FR-2.5 事实工具面 · FR-2.7/10.3 M4 与 memory_browse
                FR-8.0 轻量审查 · FR-8.1/8.2(①②④) 巩固主线
                FR-9.1 唤醒预热 · FR-6.5 被动承诺识别 · FR-4.9 CJK trigram
                → 跨会话记忆全自动流转

v4              类人性与外围：
                FR-7.1/7.2 衰减/归档/复苏 · FR-2.4 链接演化
                向量召回（bge-m3）· FR-3.1 显著性公式 · FR-3.8 确认队列
                FR-2.8 导出备份 · FR-2.9 多工作区隔离 · 人格化外围
                → 完整类人-超人混合体

贯穿线索       FR-5.x 核心协同需求：持续跟进 DSH 核心扩展点，
                成熟一个落地一个（优先 FR-5.4 外部化、FR-5.3 压缩前抽取）
```

**v2 期选题理由**：v1.1 实测暴露的最大问题不是召回质量，而是**写入端依赖模型自觉**（漏记、重复 store）与**承诺闭环断裂**（NFR-2.5 不可验收）。参照系统一致性证明自动沉淀是记忆插件的核心竞争力；卫生闸门是防污染的前置条件。这些都在插件层能力边界内，无核心依赖。

---

## 8. 验收方案（更新）

### 8.1 测试集

人格连续性测试集（约 30 条）继承 v1 六类场景，新增：

7. 弱遵循场景：模型一整轮不调任何记忆工具，关键事实是否仍被自动沉淀入库（FR-3.5 验收）；
8. 污染场景：向记忆写入乱码/复读/注入指令，闸门是否拒写、注入是否剥离（FR-3.6/3.7 验收）；
9. 承诺闭环场景：承诺到期 → 主动提起 → 用户确认完成 → `memory_close_commitment` 关闭 → 不再注入（FR-6.4 验收）；
10. 缓存友好场景：连续两轮无记忆变化，注入快照是否零追加（NFR-3.2 验收）。

### 8.2 评分方式

严格匹配/程序化断言，不用 LLM-as-judge（继承 v1）。

### 8.3 验收指标

| 指标 | 目标 |
|---|---|
| 热路径检索 p95 | ≤ 300ms |
| 记忆注入预算 | ≤ 窗口 10% |
| 指针回溯成功率 | ≥ 99%（v3 起验收） |
| 承诺闭环率 | 100%（v2 起可验收） |
| 事实检索准确率 | ≥ 95%（v3 起验收） |
| 感知响应时间增幅 | ≤ 10% |
| 自动沉淀每日 LLM 调用 | ≤ 配置上限 |

---

## 9. 风险与缓解（更新）

| 风险 | 影响 | 缓解 |
|---|---|---|
| 压缩摘要漂移 | 长任务可靠性 | 压缩前抽取 + M4 原文 + pin（依赖核心协同，跟进 FR-5.x） |
| 写入膨胀噪声 | 召回质量 | FR-3.5 门控链 + FR-3.6 闸门 + FR-4.6 相关性下限 |
| 召回张冠李戴 | 人格出戏 | 相关性下限宁缺毋滥 + 低置信标注（FR-3.4） |
| **自动沉淀 subagent 成本失控** | API 费用 | NFR-1.5 每日上限 + 冷却 + 最小字符门控 + 非工作时间冷却翻倍（参照 auto-memory） |
| **记忆内容携带注入指令回流 prompt** | 安全 | FR-3.6/3.7 双关口闸门；外部内容只注路径不注内容（参照 auto-memory 链接模式） |
| **自动沉淀串扰子代理/多会话** | 记忆污染 | 只处理顶层会话轮、按轮去重、防重入锁（参照 auto-memory 门控链） |
| embedding 本地性能不足 | 热路径延迟 | 向量降为 P2 增强，降级链保底（NFR-2.2） |
| 巩固流程污染记忆 | 长期质量 | 原始记录不可变；巩固只改派生层；可回放 |
| 隐私泄露 | 合规 | memory_forget 级联 + 全本地 + 敏感段不注入 |
| **DSH 核心扩展点不开放** | FR-5.x 停滞 | 插件侧先做等价物（pin 常驻、prompt 层任务栈）；核心协同需求单独跟进 |

---

## 10. 一句话总览（更新）

> 常驻的自我（M1）+ 不失约的承诺表 + 便签式当下（M0）+ 会生长的往事网络（M2）+ 不可篡改的双时间事实（M3）+ 永不丢失的深时存档（M4）；**每轮自动沉淀不靠自觉，入库出库双闸门防污染，注入静态动态分离保缓存**；快路径只读且可降级、重活全在暗处、夜里自我整理、会淡忘但找得回、能精确删除；上下文是有损显示层，存储是无损真相层。
