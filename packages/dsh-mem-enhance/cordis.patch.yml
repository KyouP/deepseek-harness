# The dsh-mem-enhance bundle patch: mounts the H-MEM persona memory plugin as one
# insert row over whatever the earlier bundle layers established. The row's
# identity is stable; profiles and overlays restate `config` to tune it.

- insert:
    - id: dsh-mem-enhance
      name: 'dsh-mem-enhance'
      config: {}
