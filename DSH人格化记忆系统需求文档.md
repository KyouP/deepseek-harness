# DSH 人格化记忆系统（H-MEM）需求文档

- 版本：v1.0
- 日期：2026-08-18
- 状态：草案（待评审）
- 项目代号：H-MEM（Hybrid Persona Memory），基于 DSH 插件化架构

---

## 1. 项目背景与目标

### 1.1 背景

DSH（DeepSeek Harness）是一个「一切皆插件」的 agent 运行时项目，具备自定义 Agent 预设、生命周期 Hooks（PreToolUse / PostToolUse 瀑布式中间件链）、任务调度、内置 SQLite（FTS5 全文索引）会话存储、Web UI 与桌面宠物等扩展点。社区已有多个记忆类插件（memory-evolve、plugin-recall、hermes-memory）可作参考。

本项目目标：以**记忆子系统为切入点**，在 DSH 上构建一个具备真实人格形象的拟人化 agent。人格的稳定感主要来自记忆连续性，因此记忆子系统是整个人格工程的地基。

### 1.2 总体目标

构建一套 **「超人级可靠性 + 类人级体感 + 工程级性能约束」** 的混合记忆系统：

- **优于人**：记忆精确、不可篡改、容量无上限、可备份移植、可精确删除；
- **接近人（或高智商动物）**：有选择地记忆（显著性）、会淡忘（衰减）、会联想（链接网络）、情绪影响记忆权重、空闲时自我整理（睡眠巩固）；
- **弥补生物记忆的主要缺陷**：错误重构、不可控遗忘、容量有限、无法备份、联想检索易失败。

### 1.3 三条设计公理

1. **上下文是有损显示层，存储是无损真相层** —— 一切离开上下文的内容必须先落存储，指针随行。
2. **一切注入物都有价格，预算装箱，高优先级不可挤压** —— 不存在无界注入路径，上下文爆炸在结构上不可能。
3. **丢失的判定标准是「找不回」，不是「想不起」** —— 想不起是召回率问题（可持续优化）；找不回是可靠性事故（不允许发生）。

---

## 2. 名词定义

| 术语 | 定义 |
|---|---|
| M0 工作记忆 | 当前会话的对话原文 + 会话便签（scratchpad），生命周期为会话 |
| M1 核心记忆 | 常驻上下文的 persona block 与 human block，模型可自编辑 |
| M2 情节记忆 | 事件卡片网络，卡片间语义互链，带显著性与强度 |
| M3 语义记忆 | 双时间事实图谱（事实/偏好/知识，变化而非覆盖） |
| M4 深时记忆 | 完整历史与归档记忆，仅以指针形式出现于上下文 |
| 热路径 | 响应前的同步操作，直接占用用户等待时间 |
| 温路径 | 响应返回后的异步操作，用户无感知 |
| 冷路径 | 空闲/定时执行的离线批处理 |
| 睡眠巩固 | 空闲时对记忆进行蒸馏、冲突消解、链接演化、衰减结算的后台流程 |
| 承诺追踪器 | 独立特权通道，记录并闭环「自己答应过的事」 |
| 锚点（Pin） | 以原文形式钉在上下文中、不参与压缩的关键信息 |
| 两级表示 | 每条记忆同时保存「一行摘要」与「全文」，默认只注入摘要 |

---

## 3. 功能需求

优先级：P0 = 必须（v1 即交付）；P1 = 重要（v2/v3）；P2 = 增强（v4+）。

### 3.1 人格定义层

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-1.1 | 提供结构化角色卡（性格、价值观、口癖、行为边界、禁忌话题），作为人格的事实来源 | P0 |
| FR-1.2 | persona block 常驻上下文（预算约 800 tokens），任何情况下不可压缩、不可裁剪 | P0 |
| FR-1.3 | 模型可通过工具自编辑 persona block（如用户要求改变称呼时） | P0 |
| FR-1.4 | 角色卡、口癖示例、禁用词表可打包为独立 DSH 插件 bundle 分发 | P1 |

### 3.2 记忆分层存储

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-2.1 | M0：维护会话内 scratchpad，模型可随手记录临时推断；会话结束交由巩固流程判定去留 | P0 |
| FR-2.2 | M1 human block：用户画像 + 关系状态（认识时长、最近话题、待办约定），常驻约 700 tokens，可自编辑 | P0 |
| FR-2.3 | M2：事件卡片结构 `{内容, 上下文描述, 关键词, 情绪标签, 显著性, 强度, 链接[], 双时间戳}` | P1 |
| FR-2.4 | M2：新卡片入库时自动与历史卡片建立语义链接；新信息可更新旧卡片的上下文描述（链接演化） | P2 |
| FR-2.5 | M3：事实以双时间三元组存储（`valid_from / valid_to / recorded_at`），新事实使旧事实**失效而非删除** | P1 |
| FR-2.6 | M3：原始记录不可变，派生解释可更新——记忆篡改在结构上不可能 | P1 |
| FR-2.7 | M4：完整会话原文与归档记忆永久留存，可通过工具按会话/时间范围取回 | P1 |
| FR-2.8 | 记忆导出/备份/迁移能力（弥补生物记忆不可移植的缺陷） | P2 |

### 3.3 写入门控（显著性评分）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-3.1 | 显著性评分：`S = w₁·情绪强度 + w₂·新颖度 + w₃·重复提及次数 + w₄·用户显式标记` | P1 |
| FR-3.2 | 三档处理：`S < 阈值₁` 丢弃；`阈值₁ ≤ S < 阈值₂` 暂存 scratchpad；`S ≥ 阈值₂` 异步入库 | P1 |
| FR-3.3 | 用户显式说「记住」= 最高权重，必入库且永久 pin 不衰减 | P0 |
| FR-3.4 | 抽取置信度低的事实标记「不确定」，注入时带前缀提示，不伪装成确定事实 | P1 |

### 3.4 记忆召回

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-4.1 | 混合召回：向量 + FTS 关键词 + 图邻域三路并行，融合排序 | P1 |
| FR-4.2 | 排序公式：`score = α·语义相似度 + β·关键词命中 + γ·强度(衰减后) + δ·链接邻近度` | P1 |
| FR-4.3 | 召回漏斗：粗召回（约 50 候选，零注入）→ 精排（保留约 10，相关性下限截断 + 同事件聚簇去重）→ 预算装箱 | P1 |
| FR-4.4 | 两级表示：默认只注入一行摘要（约 30 tokens/条），模型可调 `memory_expand(id)` 取全文 | P0 |
| FR-4.5 | 预算装箱：按 `score / token_cost` 贪心装入硬预算 B；装不下的降级为指针行。预算是硬约束，召回条数是结果 | P0 |
| FR-4.6 | 相关性下限：低于阈值不注入，即使预算有富余（宁缺毋滥，防张冠李戴） | P1 |
| FR-4.7 | 长卡片按需做查询聚焦压缩后再注入（温路径预计算） | P2 |

### 3.5 工作记忆上下文管理（防长任务爆炸）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-5.1 | 三段式上下文：头部（系统 + M1，永不压缩）/ 中部（压缩区：增量摘要 + 锚点 + 指针目录）/ 尾部（最近 N 轮原文窗口） | P0 |
| FR-5.2 | 增量压缩：窗口占用达 70% 触发；只将滑出部分 merge 进已有摘要，不全量重写 | P1 |
| FR-5.3 | 压缩前必抽取：滑出内容先经事实抽取入 M2/M3、原文沉 M4，再出上下文——有损显示、无损存储 | P0 |
| FR-5.4 | 中间产物外部化：超阈值工具返回默认落工作区存储，上下文只留「引用句柄 + 三行摘要」，按需 `read(handle, range)` 取片段 | P1 |
| FR-5.5 | 任务栈：长任务维护 subgoal 栈（当前子目标 + 各子目标一行结论），已完成子目标过程上下文释放进存档 | P1 |
| FR-5.6 | 锚点 Pin：关键决定/约束/未完成承诺以原文钉住，不参与压缩 | P0 |

### 3.6 承诺追踪器（特权通道）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-6.1 | 温路径专项识别承诺/约定/待办，独立成表：`{内容, 承诺对象, 期限, 状态}` | P0 |
| FR-6.2 | 活跃承诺表注入优先级 P0，永不被预算挤压 | P0 |
| FR-6.3 | 到期未完成承诺在唤醒预热时浮出，角色主动提起（闭环） | P1 |

### 3.7 遗忘与归档

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-7.1 | 衰减模型：`强度(t) = 初始强度 × e^(-λ·Δt) × Σ 访问强化`；被检索命中即强化 | P2 |
| FR-7.2 | 强度低于归档阈值 → 沉 M4（不删除，可复苏）；M4 中被命中 → 浮回 M2 | P2 |
| FR-7.3 | `memory_forget(id)` 精确删除 + 级联清理派生事实，立即生效（隐私刚需，超人能力） | P1 |
| FR-7.4 | 用户显式标记的记忆永久 pin，不参与衰减 | P0 |

### 3.8 睡眠巩固（冷路径）

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-8.1 | 触发条件：会话结束 N 分钟无新交互，或定时（如每日凌晨） | P1 |
| FR-8.2 | 巩固流水线：① 会话蒸馏（scratchpad + 对话 → 事件卡片入 M2）② 冲突消解（更新有效期区间，不覆盖）③ 链接演化 ④ human block 重编译（自编辑修正保留）⑤ 衰减结算 ⑥ 归档/复苏 | P1（①②④）/ P2（③⑤⑥） |
| FR-8.3 | 巩固结果可被人格化表达（如次日主动提起「我昨天想了想我们聊的方案」） | P2 |

### 3.9 唤醒预热

| 编号 | 需求 | 优先级 |
|---|---|---|
| FR-9.1 | 会话开始时预取当日相关记忆：纪念日、临期承诺、最近未完结话题 | P1 |

### 3.10 工具接口（模型可调）

| 编号 | 工具 | 说明 | 优先级 |
|---|---|---|---|
| FR-10.1 | `memory_recall(query)` | 主动检索长期记忆 | P0 |
| FR-10.2 | `memory_expand(id)` | 展开某条记忆全文 | P0 |
| FR-10.3 | `memory_browse(session, range)` | 翻 M4 历史原文 | P1 |
| FR-10.4 | `memory_store(content, ...)` | 显式写入 | P0 |
| FR-10.5 | `memory_update_core(block, content)` | 自编辑 persona/human block | P0 |
| FR-10.6 | `memory_forget(id)` | 精确遗忘 | P1 |

### 3.11 人格化外围（后续阶段，非本期范围）

生命节律（情绪状态机、作息感、主动问候）、具身表现（桌宠/皮肤/TTS 声线）、一致性守护（出戏检测打分、反馈校准）。本期仅预留 Hook 接口，不实现。

---

## 4. 非功能需求

### 4.1 性能

| 编号 | 指标 | 目标值 |
|---|---|---|
| NFR-1.1 | 热路径同步检索延迟 | p95 ≤ 300ms |
| NFR-1.2 | 记忆注入 token 总预算 | ≤ 上下文窗口的 10% |
| NFR-1.3 | 写入/抽取/巩固 | 全部在温路径或冷路径，热路径零写入 |
| NFR-1.4 | 记忆模块整体 overhead | 用户可感知响应时间增幅 ≤ 10%（允许略慢，不允许明显拖累） |

### 4.2 可靠性与降级

| 编号 | 需求 | 目标值 |
|---|---|---|
| NFR-2.1 | 召回超时 → 降级为仅 M0+M1 回复（人格在线，仅「一时没想起细节」） | 必须 |
| NFR-2.2 | embedding 不可用 → 退化纯 FTS5 关键词检索 | 必须 |
| NFR-2.3 | 全部外部依赖失效 → M0+M1 保底，系统永不因记忆模块失能 | 必须 |
| NFR-2.4 | 指针回溯成功率（经工具找回任意历史细节） | ≥ 99% |
| NFR-2.5 | 承诺闭环率（到期承诺被主动提起比例） | 100% |
| NFR-2.6 | 事实检索准确率 | ≥ 95% |

### 4.3 部署约束

- 全部组件本地可运行，零外部服务依赖起步；
- 结构化存储用 SQLite（DSH 内置，含 FTS5）；向量检索用本地小 embedding 模型起步，可平滑替换；
- 以纯 ESM 插件 bundle 形式交付，`dsh plugin add` 可安装。

---

## 5. 解决思路

### 5.1 总体架构

```
                    ┌─────────────────────────────────────┐
   常驻上下文        │  M1 核心记忆（persona / human 双块）   │  ~1.5k tokens
  （每轮必注入）      │  + 活跃承诺表（P0，不可挤压）          │
                    ├─────────────────────────────────────┤
   工作记忆          │  M0 三段式上下文                      │
                    │  头部固定 / 中部压缩区+锚点+指针 / 尾部原文 │
                    ├─────────────────────────────────────┤
   按需召回          │  M2 情节记忆（事件卡片网络，互链）       │  两级表示
                    │  M3 语义记忆（双时间事实图谱）           │  预算装箱注入
                    ├─────────────────────────────────────┤
   指针式            │  M4 深时记忆（完整历史，可回溯）        │  仅指针
                    └─────────────────────────────────────┘
```

三条速度通道：

```
通道      时机                操作                              延迟预算
──────────────────────────────────────────────────────────────────────────
热路径    响应前（同步）       M1+承诺表注入 + 一次并行混合召回      p95 ≤ 300ms
                              （超时/失败自动降级）
温路径    响应后（异步）       显著性评分 → 门控写入；事实抽取；
                              承诺识别；去重与冲突标记             用户无感知
冷路径    空闲/定时（离线）     睡眠巩固六段流水线；衰减结算；
                              归档/复苏；human block 重编译        无预算限制
```

### 5.2 三大核心矛盾的应对策略

**矛盾一：长任务导致工作记忆上下文爆炸**
→ 三段式上下文 + 增量压缩 + 压缩前抽取（有损显示、无损存储）+ 中间产物外部化（引用句柄）+ 任务栈。其中「中间产物外部化」收益最大，通常可削减长任务上下文 60% 以上。

**矛盾二：记忆召回的上下文爆炸**
→ 两级表示（默认只注入一行摘要，按需 expand）+ 预算装箱（硬预算、按 score/token 贪心、溢出降指针）+ 相关性下限截断。预算是硬约束，召回条数是结果，爆炸路径在结构上被消除。

**矛盾三：记忆丢失**
→ 分三条独立路径封堵：压缩丢失（压缩前抽取 + 原文沉 M4 + 指针回溯）；召回遗漏（多路召回 + 承诺追踪器特权通道）；衰减丢失（显式标记永久 pin + M4 只归档不删除）。验收不看「压缩保留率」，看「指针回溯成功率」与「承诺闭环率」。

### 5.3 注入优先级体系（总阀门）

```
P0  不可挤压：M1 双 block + 活跃承诺表          ~2000 tok
P1  当前任务：subgoal 栈 + 锚点                  动态
P2  本轮召回：预算装箱后的记忆                   ≤ 3000 tok
P3  可牺牲：背景便签、低分召回、旧摘要            首先被裁

上下文逼近上限时从 P3 向上裁剪；P0 任何情况下不动。
```

---

## 6. 技术路径

### 6.1 与 DSH 的集成点

| DSH 能力 | 用途 |
|---|---|
| 自定义 Agent 预设 | 角色卡与 persona block 的载体 |
| PreToolUse / PostToolUse Hooks | 温路径写入、承诺识别、出戏检测预留点 |
| 瀑布式中间件链 | 情绪状态、响应后校验的挂载点 |
| session-query-sqlite（FTS5） | M4 历史检索与关键词召回通道 |
| 任务调度 | 睡眠巩固定时触发、唤醒预热 |
| 插件 bundle（工具+技能+提示词+工作流） | 整体交付形态，`dsh plugin add` 安装 |

### 6.2 存储与检索选型

| 组件 | 起步方案 | 升级路径 |
|---|---|---|
| 结构化存储 | SQLite（卡片表 + 事实三元组表 + 链接边表 + 承诺表） | 长期不变 |
| 全文检索 | SQLite FTS5 | 不变 |
| 向量检索 | 本地小 embedding 模型 | 换更强模型仅需重建索引 |
| 融合排序 | RRF 或加权 score 公式 | 加学习时间衰减权重 |
| 图谱 | SQLite 边表模拟 | 数据量大后再评估专用图库 |

### 6.3 关键算法

- 显著性评分：四因子加权，阈值双档（见 FR-3.1/3.2）
- 衰减与强化：指数衰减 + 访问强化（见 FR-7.1）
- 双时间事实：`valid_from / valid_to / recorded_at`，冲突时更新区间而非覆盖（见 FR-2.5）
- 增量压缩：摘要 + 滑出段 → 新摘要，成本 O(滑出部分)（见 FR-5.2）
- 预算装箱：score/token 贪心 + 指针降级（见 FR-4.5）

---

## 7. 分期实施路线

```
v1（1~2 天）   M1 双 block + 自编辑工具 + M0 scratchpad
               + 承诺追踪器 + 两级表示 + 三段式上下文骨架
               → 已有「稳定人格 + 记住用户 + 不失约」的体感

v2（3~5 天）   M2/M3 入库 + 混合召回 + 召回漏斗 + 预算装箱
               + 热路径降级策略 + 中间产物外部化
               → 跨会话记忆上线，性能 SLA 就位

v3（约 1 周）  显著性门控 + 冲突消解 + 睡眠巩固流程 + 唤醒预热
               → 记忆开始自我整理，质量跃升

v4             衰减/遗忘/复苏 + 链接演化 + 精确遗忘级联
               + 导出备份 + 人格化外围（情绪、桌宠、声线）
               → 完整的类人-超人混合体
```

**v1 范围说明**：三段式上下文、承诺追踪器、两级表示是防爆炸的骨架，必须在 v1 就位；召回漏斗、衰减等是后期往骨架上填的肉。

---

## 8. 验收方案

### 8.1 测试集

自建人格连续性测试集（约 30 条），覆盖：

1. 三周前的约定是否记得并主动闭环；
2. 改过的偏好是否记得旧版（双时间验证）；
3. 被要求忘掉的事是否彻底不再出现（含派生事实级联）；
4. 长任务（100+ 轮）后关键约束是否仍被遵守；
5. 检索延迟与 token 开销是否达标；
6. 故意注入的错误信息是否被「不确定」机制正确标注。

### 8.2 评分方式

采用严格匹配/程序化断言，**不用 LLM-as-judge**（已有研究表明 LLM 裁判会放过相当比例的故意错误答案）。

### 8.3 验收指标汇总

| 指标 | 目标 |
|---|---|
| 热路径检索 p95 | ≤ 300ms |
| 记忆注入预算 | ≤ 窗口 10% |
| 指针回溯成功率 | ≥ 99% |
| 承诺闭环率 | 100% |
| 事实检索准确率 | ≥ 95% |
| 感知响应时间增幅 | ≤ 10% |

---

## 9. 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| 压缩摘要漂移导致关键信息失真 | 长任务可靠性 | 压缩前抽取 + M4 原文 + 锚点 pin 关键约束 |
| 写入膨胀导致检索噪声 | 召回质量 | 显著性门控 + 相关性下限 + 衰减归档 |
| 召回张冠李戴 | 人格出戏 | 相关性下限宁缺毋滥 + 低置信标注 |
| embedding 模型本地性能不足 | 热路径延迟 | 降级链设计（FTS5 兜底）+ 预算截断保底 |
| 巩固流程出错污染记忆 | 长期记忆质量 | 原始记录不可变；巩固只改派生层；可回放 |
| 隐私数据泄露 | 合规 | memory_forget 级联删除 + 全本地存储 |

---

## 10. 一句话总览

> 常驻的自我（M1）+ 不失约的承诺表 + 便签式当下（M0 三段式）+ 会生长的往事网络（M2）+ 不可篡改的双时间事实（M3）+ 永不丢失的深时存档（M4）；快路径只读且可降级、重活全在暗处、夜里自我整理、会淡忘但找得回、能精确删除；上下文是有损显示层，存储是无损真相层。
